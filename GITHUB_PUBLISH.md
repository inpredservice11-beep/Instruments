# Инструкция по публикации проекта на GitHub

## Шаг 1: Подготовка проекта

Убедитесь, что все файлы готовы к публикации:
- ✅ `README.md` обновлен
- ✅ `.gitignore` настроен (уже есть)
- ✅ Все необходимые файлы на месте

## Шаг 2: Создание репозитория на GitHub

1. Перейдите на [GitHub.com](https://github.com) и войдите в свой аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните форму:
   - **Repository name**: `tool-management-system` (или другое название)
   - **Description**: "Система учета выдачи и возврата инструмента"
   - Выберите **Public** (публичный) или **Private** (приватный)
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже есть эти файлы)
4. Нажмите **"Create repository"**

## Шаг 3: Инициализация Git в локальном проекте

Откройте терминал/командную строку в папке проекта и выполните:

```bash
# Настройка Git для правильной обработки окончаний строк (рекомендуется)
git config core.autocrlf true

# Или для всех проектов глобально:
# git config --global core.autocrlf true

# Инициализация git репозитория
git init

# Добавление всех файлов (кроме тех, что в .gitignore)
git add .

# Первый коммит
git commit -m "Initial commit: Система учета инструментов"
```

**Примечание:** Файл `.gitattributes` уже создан в проекте и автоматически настроит правильные окончания строк для разных типов файлов.

## Шаг 4: Подключение к удаленному репозиторию

После создания репозитория на GitHub, вы увидите инструкции. Выполните:

```bash
# Замените YOUR_USERNAME на ваш GitHub username
# Замените REPOSITORY_NAME на название вашего репозитория

git remote add origin https://github.com/YOUR_USERNAME/REPOSITORY_NAME.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/REPOSITORY_NAME.git
```

## Шаг 5: Отправка кода на GitHub

```bash
# Отправка кода в репозиторий
git branch -M main
git push -u origin main
```

Если GitHub попросит авторизацию:
- Для HTTPS: используйте Personal Access Token (см. ниже)
- Для SSH: убедитесь, что SSH ключ настроен

## Шаг 6: Настройка Personal Access Token (для HTTPS)

Если используете HTTPS и GitHub требует токен:

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Нажмите **"Generate new token"**
3. Выберите права: `repo` (полный доступ к репозиториям)
4. Скопируйте токен (он показывается только один раз!)
5. Используйте токен вместо пароля при `git push`

## Альтернативный способ: Через GitHub Desktop

Если предпочитаете графический интерфейс:

1. Скачайте [GitHub Desktop](https://desktop.github.com/)
2. Установите и войдите в аккаунт
3. File → Add Local Repository → выберите папку проекта
4. Нажмите "Publish repository" в GitHub Desktop
5. Выберите название и нажмите "Publish Repository"

## Что будет опубликовано

Благодаря `.gitignore`, следующие файлы **НЕ** будут опубликованы:
- `tool_management.db` (база данных)
- `window_config.json` (персональные настройки)
- `__pycache__/` (кэш Python)
- Файлы IDE и временные файлы

Будут опубликованы:
- ✅ Исходный код (`app.py`, `database_manager.py`, `dialogs.py`, и т.д.)
- ✅ `README.md`
- ✅ `requirements.txt`
- ✅ SQL скрипты из папки `database/`
- ✅ `.gitignore`

## Дальнейшие обновления

После первой публикации, для обновления кода:

```bash
# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push
```

## Полезные команды Git

```bash
# Проверить статус файлов
git status

# Посмотреть историю коммитов
git log

# Посмотреть изменения
git diff

# Отменить изменения в файле (до git add)
git checkout -- filename

# Отменить git add
git reset HEAD filename
```

## Добавление описания репозитория

После публикации на GitHub:

1. Перейдите на страницу репозитория
2. Нажмите на шестеренку ⚙️ рядом с "About"
3. Добавьте:
   - **Topics**: `python`, `tkinter`, `sqlite`, `tool-management`, `inventory`
   - **Website**: если есть сайт
   - **Description**: краткое описание проекта

## Добавление лицензии (опционально)

Если хотите добавить лицензию:

1. Создайте файл `LICENSE` в корне проекта
2. Или используйте GitHub: Settings → General → License → Choose a license
3. Рекомендуется: MIT License (уже упомянута в README)

## Проверка

После публикации проверьте:
- ✅ Все файлы видны на GitHub
- ✅ README.md отображается корректно
- ✅ `.gitignore` работает (база данных не видна)
- ✅ Код форматирован правильно

---

**Готово!** Ваш проект теперь на GitHub! 🎉

