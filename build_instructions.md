# 📦 Компиляция ToolManagement в exe

## 🚀 Быстрый старт

### Вариант 1: Использование скрипта (Рекомендуется)

1. **Установите PyInstaller:**
   ```bash
   pip install pyinstaller
   ```

2. **Запустите сборщик:**
   ```bash
   python build_exe.py
   ```

3. **Выберите вариант:**
   - **1** - Одиночный exe файл (рекомендуется)
   - **2** - Портативная версия (папка)
   - **3** - Создать установщик

### Вариант 2: Ручная компиляция

```bash
# Установка PyInstaller
pip install pyinstaller

# Компиляция в один exe файл
pyinstaller --onefile --windowed --name ToolManagement \
  --add-data "database;database" \
  --add-data "photos;photos" \
  --add-data "window_config.json;." \
  --hidden-import tkinter \
  --hidden-import tkinter.ttk \
  --hidden-import PIL \
  --hidden-import tkcalendar \
  --hidden-import reportlab \
  --hidden-import openpyxl \
  --hidden-import matplotlib \
  --hidden-import barcode \
  app.py
```

## 📋 Что включает exe файл

✅ **Все зависимости Python** (tkinter, PIL, matplotlib и др.)
✅ **База данных** (создается автоматически при первом запуске)
✅ **Тестовые данные** (сотрудники, инструменты, фото)
✅ **Файлы конфигурации** (настройки окон)
✅ **Все фотографии** (инструментов и сотрудников)

## 🎯 Использование exe файла

1. **Распространение:** Просто скопируйте `ToolManagement.exe` на другой компьютер
2. **Первый запуск:** Приложение автоматически создаст базу данных с тестовыми данными
3. **Работа:** Полная функциональность без установки Python

## 📁 Структура после сборки

```
dist/
├── ToolManagement.exe          # Основной exe файл
└── ToolManagement_Portable/    # Портативная версия (если выбрана)
    ├── ToolManagement.exe
    ├── database/
    ├── photos/
    └── ...
```

## 🔧 Требования для сборки

- **Python 3.7+**
- **PyInstaller:** `pip install pyinstaller`
- **Все зависимости:** `pip install -r requirements.txt`

## 🐛 Устранение проблем

### PyInstaller не найден
```bash
pip install pyinstaller
```

### Ошибки импортов
Убедитесь, что установлены все зависимости:
```bash
pip install -r requirements.txt
```

### Большой размер exe
Это нормально - exe включает все зависимости Python (~20-50 МБ)

### exe не запускается
- Проверьте наличие Visual C++ Redistributable на целевом ПК
- Попробуйте портативную версию вместо одного файла

## 📞 Поддержка

Если возникли проблемы со сборкой:
1. Проверьте логи PyInstaller в консоли
2. Убедитесь, что все файлы на месте
3. Попробуйте разные версии PyInstaller

---

**Примечание:** exe файл не требует установки и работает на Windows без Python!
